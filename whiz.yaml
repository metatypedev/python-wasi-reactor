reactor_guest:
  watch: "src/**/*"
  command: "./dev/build.sh"

wasmedge_host:
  watch: "wasmedge/**/*.rs"
  command: "cargo run -p wasmedge_reactor"
  depends_on:
    - reactor_guest

bindgen:
  command: deno_bindgen -- -p wasmedge_reactor -F deno
  depends_on:
    - wasmedge_host